/**
 * Layer for classes representing data only, e.g. rows of a relational database,
 * documents in a NoSQL database, etc. Commonly referred to as data transfer
 * objects (DTO) or value objects (VO).
 */
package com.ran488.dao.dto;